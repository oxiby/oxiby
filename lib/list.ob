pub struct List<t> {
    pub fn times(n: Integer, f: f) -> List<t> where f is Fn() -> t {
        let list = []

        for i in (1..=n) {
            list.push(f.call())
        }

        list
    }

    pub fn sample(self) -> t {
        self.sample()
    }
}
