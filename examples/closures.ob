fn main() {
    let null = fn() {}

    null()

    let implicit = fn(name) {
        "Hello, #{name}!"
    }

    print_line(implicit("Oxiby"))

    let explicit = fn(name: String) -> String {
        "Hello, #{name}!"
    }

    print_line(explicit("Oxiby"))
}
